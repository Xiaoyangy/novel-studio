package agents

const arcRehearsalCapabilityPromptV1 = `
执行能力门禁：input.execution_capabilities是Host按本次实际配置冻结的API清单，不是世界已发生事实。actor_ref/recipient_ref只用world_state.actors[].agent_id，resource_refs只用world_state.resources[].resource_id；mechanism存在不等于该执行API存在，actor_scope里的背景岗位不是正式actor。每项available的material_check须填写非空capability_requirements，逐一声明所选路径确需的动作类型、实际执行者、目标资源、接收者及公开机制。无目标ID的水位实测、无执行适配器的背景安全确认或背景签收必须missing/unclear；不能用M_SAIL文字、resource_use/self_work或一般陈述替代外部观察/读数/许可。self_work只表示本人动作和进度；communication只是有来源的陈述/请求，不能生成背景回复或独立事实；operational_observation仅支持既有定性非文书资源的局部可用性，不支持整船安全合格或开航许可。resource_use仅表示已存在资源的实际使用，不产生新观测知识。
capability_requirements按前置顺序排列；key是报告内唯一短名，不是resource_id。depends_on只能引用更早的key；审核保留草案已有依赖的完整身份与字段，不删除/改类/降成not_required，可增加遗漏操作。missing/unclear可以保留空目标但不能造ID；未选可选分支放unresolved_items。现有权限、未来同意、移动工时与实际结果仍是条件，接口可用不表示已经执行或保证成功。
支持当前artifact_write/read/sign，不把未来文书冒充当前资源：首次artifact_write使用唯一key和现有material_inputs，不填新resource_id；后续artifact_ref引用更早write的key，并在depends_on显式列它。不同角色读取/签认前须有同一artifact_ref的resource_delivery依赖；签认另须本人对同一预期版本的artifact_read（作者已知本人所写内容）。修改未来版本用artifact_write引用先前artifact_ref生成新key，仅原作者可改，不重复投入材料；读取/签认必须引用该精确预期版本。当前已存在产物用现有resource_refs，宿主核其artifact类型。报告内未来产物不建立真实资源、版本或签名，正式角色阶段仍须原有输出/交付/读取/签认工具逐项执行。此静态检查只验证显式依赖，不宣称穷尽自然语言遗漏；所有关键操作必须诚实分类。`
